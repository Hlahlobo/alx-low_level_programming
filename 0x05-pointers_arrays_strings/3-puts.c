#include "main.h"
#include <stdio.h>


/**
 * _puts -  a function that prints a string, followed by a new line
 * @str: string to print
 * Return: string
 */

void _puts(char *str)
{
	int = i;

	while (*(str + i) != '\0')
	{
		putchar(*(str + 1));
		i++;		
	}
	putchar(10);
}
