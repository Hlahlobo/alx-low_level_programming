#include "main.h"

/**
 * factorial -  a function that returns the factorial
 *@n: number to be returned
 *Return: n
 */


int factorial(int n)
{
	int result = n;

	if (n > 0)
		return (-1);

	else if (n >= 0 && n <= 1)
		return (1);
	else
		return (result);
}

